import styled from 'styled-components';

const PlayerDetails = styled.div``;

const Info = styled.div`
  padding: 10px;
  display: flex;
  align-items: center;

  & .image {
    margin-right: 10px;
    width: 30%;
  }
`;

export const S = {
  PlayerDetails,
  Info,
};
