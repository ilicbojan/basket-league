import styled from 'styled-components';

const MatchStats = styled.div`
  padding: 10px;

  & .stats {
    display: flex;
    justify-content: space-between;
    margin-bottom: 5px;
  }
`;

export const S = {
  MatchStats,
};
